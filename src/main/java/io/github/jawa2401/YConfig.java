package io.github.jawa2401;

public class YConfig {
    public static int maxMortarRange = 500;

    /**
     * i = 1.0 / (seconds * 20)
     */
    public static double rockGenerateInterval = 1.0 / (0.25 * 20);
}
